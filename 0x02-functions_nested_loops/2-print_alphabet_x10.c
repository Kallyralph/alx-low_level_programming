#include "main.h"

/**
 * print_alphabet_x10 - Male alphabet x10 times
 */
void print_alphabet_x10(void)
{

	char c;
	int i =1;

	while (i <= 1)
	{
		for (c ='a'; c <= 9; i++)
		for (c = 'a'; c <= 'z'; c++)
		_putchar('\n');	
